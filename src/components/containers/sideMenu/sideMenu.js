import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div``;

const SideMenu = (props) => {
    return (
        <StyledWrapper>Side Menu</StyledWrapper>
    )
}

export default SideMenu;