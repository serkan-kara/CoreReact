import React from 'react';
import styled from 'styled-components';

const StyledWrapper = styled.div``;

const SidePanel = (props) => {
    return (
        <StyledWrapper>Side panel</StyledWrapper>
    )
}

export default SidePanel;